import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sonalijewellers.erp',
  appName: 'Sonali Jewellers',
  webDir: 'dist'
};

export default config;
